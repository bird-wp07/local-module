import { describe, test } from "mocha";
import { expect } from "chai";

describe(`Is this the real life?`, () => {
    test(`Is this just fantasy?`, () => {
        expect(1).to.equal(1);
    });
});
